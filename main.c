#include <stdio.h>
#include <stdlib.h>
#include "Compte.h"
/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	 //RemplirUnCompte(); //valide
//	RemplirPlusieurComptes(3);// valide
	
	DepotMontant(3,26);
	return 0;
}
